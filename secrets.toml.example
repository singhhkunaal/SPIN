# ============================================================================
# DO NOT COMMIT THIS FILE WITH REAL VALUES.
# This is a TEMPLATE. The real secrets live in Streamlit Cloud's secrets panel
# (https://share.streamlit.io → your app → Settings → Secrets), NOT in git.
#
# For local development only: copy this file to .streamlit/secrets.toml
# (without the .example) and fill in real values. The .gitignore in this repo
# already excludes .streamlit/secrets.toml so it cannot be committed.
# ============================================================================

CLAUDE_API_KEY = "sk-ant-api03-..."

# Supabase — get these from your project's Settings → API
SUPABASE_URL = "https://xxxxxxxxxxxxxx.supabase.co"
SUPABASE_KEY = "eyJhbGciOiJIUzI1NiIsInR5..."  # use the SERVICE_ROLE key, not the anon key
